import Styled from 'styled-components';

export const StyledResumenCompra = Styled.div`
    // Box
    box-shadow: rgba(0, 0, 0, 0.1) 0px 10px 15px -3px, rgba(0, 0, 0, 0.05) 0px 4px 6px -2px;

    // Background
    background-color: var(--container-color);

    // Bordes
    border-radius: 20px;

    // Padding
    padding-left: 2rem;
    padding-right: 2rem;
`;